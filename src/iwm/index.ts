/**
 * IWM (Inventory & Workflow Management) Functions
 * 
 * Exports all IWM-specific Firebase Functions
 */

// Export IWM scripts (if they exist)
// Note: Scripts are typically not exported as Firebase Functions
// They are run manually or via admin SDK
