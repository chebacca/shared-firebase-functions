# Vision API (OCR)

Server-side text extraction via Google Cloud Vision API. Used by mobile companion PWA and IWM Asset Operations wizard.

## Enable Vision API (one-time)

```bash
gcloud services enable vision.googleapis.com --project=backbone-logic
```

Run from any directory where `gcloud` is authenticated with the backbone-logic project.
